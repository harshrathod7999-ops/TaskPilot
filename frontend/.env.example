# Frontend env. In dev, leave VITE_API_BASE unset to use the Vite proxy (/api).
# In production (Vercel), point it at your deployed FastAPI backend:
# VITE_API_BASE=https://taskpilot-api.onrender.com
VITE_API_BASE=/api
